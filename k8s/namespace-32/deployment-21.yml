apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-32-21
  namespace: ns-32
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: app
        image: registry.io/app-32-21:latest
        ports:
        - containerPort: 8080
        env:
        - name: ENV
          value: production
        resources:
          limits:
            memory: 512Mi
            cpu: 500m
