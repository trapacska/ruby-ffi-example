workflows:
  workflow_24:
    steps:
    - script@1:
        inputs:
        - content: |
            echo "Running workflow 24"
            echo "Step 1 of workflow 24"
            echo "Step 2 of workflow 24"
    - deploy-to-bitrise-io@2:
        inputs:
        - deploy_path: /tmp/output_24
