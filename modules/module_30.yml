workflows:
  workflow_30:
    steps:
    - script@1:
        inputs:
        - content: |
            echo "Running workflow 30"
            echo "Step 1 of workflow 30"
            echo "Step 2 of workflow 30"
    - deploy-to-bitrise-io@2:
        inputs:
        - deploy_path: /tmp/output_30
