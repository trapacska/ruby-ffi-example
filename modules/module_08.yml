workflows:
  workflow_8:
    steps:
    - script@1:
        inputs:
        - content: |
            echo "Running workflow 8"
            echo "Step 1 of workflow 8"
            echo "Step 2 of workflow 8"
    - deploy-to-bitrise-io@2:
        inputs:
        - deploy_path: /tmp/output_8
