workflows:
  workflow_32:
    steps:
    - script@1:
        inputs:
        - content: |
            echo "Running workflow 32"
            echo "Step 1 of workflow 32"
            echo "Step 2 of workflow 32"
    - deploy-to-bitrise-io@2:
        inputs:
        - deploy_path: /tmp/output_32
